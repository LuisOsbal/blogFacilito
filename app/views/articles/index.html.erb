<% @articles.each do |article| %>
	<div class="Articles-tiem">
		<h1><%= link_to article.title, article %></h1>
		<div>
			<%= article.body %> - <%= link_to "Eliminar", article, method: :delete %>
		</div>
	</div>
<% end %>